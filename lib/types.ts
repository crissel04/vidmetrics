// Full type definitions — expanded in Step 6

export interface AIInsights {
  whatIsWorking: string
  uploadPattern: string
  titleFormula: string
  gapOpportunity: string
  gapOpportunities: string[]
}
